// @syscall parsing + compilation test.

fn sys_exit(uint32 code) {
    @syscall(231, code)
}

fn sys_write(uint32 fd, uint64 buf, uint64 len) -> uint64 {
    uint64 ret = @syscall(1, fd, buf, len)
    return ret
}
