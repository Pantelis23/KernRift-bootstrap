// Test unary operators: negation, bitwise NOT, logical NOT.

fn negate(int32 x) -> int32 {
    int32 result = -x
    return result
}

fn bitwise_not(uint64 x) -> uint64 {
    uint64 result = ~x
    return result
}

fn logical_not(uint64 x) -> uint64 {
    uint64 result = !x
    return result
}
