spinlock ConsoleLock;
spinlock SchedLock;
@module_caps(PhysMap);

@ctx(irq)
@caps(PhysMap)
fn entry() {
  critical {
    allocpoint();
  }
  acquire(ConsoleLock);
  acquire(SchedLock);
  release(SchedLock);
  release(ConsoleLock);
}
