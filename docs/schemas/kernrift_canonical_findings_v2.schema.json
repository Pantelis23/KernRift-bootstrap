{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kernrift.dev/schemas/kernrift_canonical_findings_v2.schema.json",
  "title": "kernrift canonical findings v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "surface",
    "file",
    "canonical_findings",
    "findings"
  ],
  "properties": {
    "schema_version": {
      "const": "kernrift_canonical_findings_v2"
    },
    "surface": {
      "enum": ["stable", "experimental"]
    },
    "file": {
      "type": "string",
      "minLength": 1
    },
    "canonical_findings": {
      "type": "integer",
      "minimum": 0
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "function",
          "classification",
          "surface_form",
          "canonical_replacement",
          "migration_safe"
        ],
        "properties": {
          "function": {
            "type": "string",
            "minLength": 1
          },
          "classification": {
            "enum": ["compatibility_alias", "deprecated_alias"]
          },
          "surface_form": {
            "type": "string",
            "pattern": "^@"
          },
          "canonical_replacement": {
            "type": "string",
            "minLength": 1
          },
          "migration_safe": {
            "type": "boolean"
          }
        }
      }
    }
  }
}
